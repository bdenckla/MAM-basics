Look up the meaning of a MAM manuscript sigil or bibliographic abbreviation.

The shared reference now lives in `doc/sigil-decoding.md` at the repo root.

Use that file as the primary source because it is tool-neutral and is intended for Claude Code, Copilot, GPT, and manual use alike.

Important: the shared file preserves uncertainties and conflicts instead of flattening them away. In particular, do not assume the older contents of this command file were definitive.

User input: $ARGUMENTS
