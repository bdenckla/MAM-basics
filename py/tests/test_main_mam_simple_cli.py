# Initially generated by GitHub Copilot.
import unittest

import main_mam_simple


class TestMainMamSimpleCli(unittest.TestCase):
    def test_no_subcommand_defaults_to_all(self):
        args = main_mam_simple._parse_args([])
        self.assertEqual(args.command, "all")

    def test_implicit_all_accepts_book39(self):
        args = main_mam_simple._parse_args(["--book39", "Ruth"])
        self.assertEqual(args.command, "all")
        self.assertEqual(args.book39, "Ruth")

    def test_core_only_accepts_book39(self):
        args = main_mam_simple._parse_args(["core-only", "--book39", "Ruth"])
        self.assertEqual(args.command, "core-only")
        self.assertEqual(args.book39, "Ruth")

    def test_doc_only_accepts_no_book_args(self):
        args = main_mam_simple._parse_args(["doc-only"])
        self.assertEqual(args.command, "doc-only")

    def test_doc_only_rejects_book39(self):
        with self.assertRaises(SystemExit):
            main_mam_simple._parse_args(["doc-only", "--book39", "Ruth"])


if __name__ == "__main__":
    unittest.main()
