# Initially generated by GitHub Copilot.
"""Helpers for loading authored JSON snippet text from external files."""

from pathlib import Path

_SNIPPET_ROOT = Path(__file__).resolve().parent / "json_snippets"


def read_text(subdir: str, fname: str) -> str:
    """Read snippet text from py/author/json_snippets/<subdir>/<fname>."""
    with open(_SNIPPET_ROOT / subdir / fname, "r", encoding="utf-8") as infile:
        return infile.read().rstrip("\n")
