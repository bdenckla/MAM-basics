# Initially generated by GitHub Copilot.
"""
HTML document: Dual-trope - מ:כפול.

To regenerate the output HTML, run from the repo root::

    .venv\\Scripts\\python.exe py\\main_authored.py gen-mam-parsed-docs

Output goes to ../MAM-parsed/gh-pages/mpplus_dualcant.html.
"""

from author_util import author
from author_util.claim import ClaimCollection
from author import mp_cmn_json_snippets as jsnip
from author import mp_dualcant_common as dualcant_common

_FNAME = "mpplus_dualcant.html"
_TITLE = "Dual-trope"

_PLUS_DOC = "mpplus.html"

_JSON_DUALCANT = jsnip.read_text("mpplus_dualcant", "dualcant.json")


def gen_html_file(tdm_ch, claims: ClaimCollection):
    """Generate mpplus_dualcant.html in the given output directory."""
    cbody = build_body(claims=claims)
    return author.help_gen_html_file(__file__, tdm_ch, _FNAME, _TITLE, cbody)


def build_body(*, claims: ClaimCollection):
    return dualcant_common.build_body(
        claims=claims,
        back_doc=_PLUS_DOC,
        back_label="Reading $MAM-parsed-plus",
        heading_suffix="",
        claim_id="mp.plus.example.dualcant-template-object",
        claim_kind="example",
        claim_subject="mp:plus",
        example_json=_JSON_DUALCANT,
    )


def _build_body(*, claims: ClaimCollection):
    return build_body(claims=claims)
