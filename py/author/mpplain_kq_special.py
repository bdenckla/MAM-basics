# Initially generated by GitHub Copilot.
"""
HTML document: Special ketiv/qere — מ:כו״ק מיוחד (plain format).

To regenerate the output HTML, run from the repo root::

    .venv\\Scripts\\python.exe py\\main_authored.py gen-mam-parsed-docs

Output goes to ../MAM-parsed/gh-pages/mpplain_kq_special.html.
"""

from author_util import author
from author_util.claim import ClaimCollection
from author import mp_cmn_json_snippets as jsnip
from author import mp_kq_special_common as kq_special_common

_FNAME = "mpplain_kq_special.html"
_TITLE = "Special ketiv/qere (plain format)"
_CLAIM_ID_KQ_SPECIAL_PLAIN = "mp.plain.kq-special.semantic-shape"

_PLAIN_DOC = "mpplain.html"

_JSON_KQ_SPECIAL = jsnip.read_text("mpplain_kq_special", "kq_special.json")


def gen_html_file(tdm_ch, claims: ClaimCollection):
    """Generate mpplain_kq_special.html in the given output directory."""
    cbody = build_body(claims=claims)
    return author.help_gen_html_file(__file__, tdm_ch, _FNAME, _TITLE, cbody)


def build_body(*, claims: ClaimCollection):
    return kq_special_common.build_body(
        claims=claims,
        back_doc=_PLAIN_DOC,
        back_label="Reading $MAM-parsed-plain",
        heading_suffix=" (plain format)",
        claim_id=_CLAIM_ID_KQ_SPECIAL_PLAIN,
        claim_kind="struct",
        claim_subject="mp:plain",
        claim_data={
            "template": "מ:כו״ק מיוחד",
            "required_positional_params": 2,
            "required_named_params": ["סוג"],
        },
        example_json=_JSON_KQ_SPECIAL,
    )


def _build_body(*, claims: ClaimCollection):
    return build_body(claims=claims)
