# Initially generated by GitHub Copilot.
"""Internal claim-definition storage for mp_cmn."""

from typing import Any

ClaimDef = tuple[str, Any, str, str, Any]

_CLAIM_DEFS: list[ClaimDef] = []


def claim_payload(
    claim_id: str,
    payload,
    *,
    kind: str,
    subject: str,
    data: Any = None,
):
    _CLAIM_DEFS.append((claim_id, payload, kind, subject, data))
    return payload


def iter_claim_defs() -> tuple[ClaimDef, ...]:
    return tuple(_CLAIM_DEFS)
