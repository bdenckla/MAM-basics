# Initially generated by Claude Code.
"""Entrypoint wrapper for py_cam1753_loc.gen_flat_stream.

Added 2026-08-22 by Phase 3 of
``doc/PLAN-evacuate-python-from-codex-index-trio.md``, one of eight that bring every
runnable module of ``py_cam1753_loc/`` under a wrapper.  That package's modules take
their data paths from ``cam1753_paths``, which sits at the top of ``py/`` and so is
importable only when ``py/`` is ``sys.path[0]``.  A wrapper here makes that so;
``python py/py_cam1753_loc/gen_flat_stream.py`` does not, putting ``py/py_cam1753_loc/`` there
instead.

The name is the rule codex-index-aleppo's Phase 3 settled the day before:
``main_cam1753_`` plus the module stem.  ``py_ac_loc``'s counterpart of this module,
where there is one, is reached as ``main_ac_`` plus the same stem.
"""

from py_cam1753_loc.gen_flat_stream import main

if __name__ == "__main__":
    main()
