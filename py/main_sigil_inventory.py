# Initially generated by GitHub Copilot.
import json
import sys

from pysigils import inventory


def almost_main():
    sys.stdout.reconfigure(encoding="utf-8")
    out_path = "out/sigil-inventory.json"
    result = inventory.write_inventory(out_path)
    counts = json.dumps(result["header"]["counts"], ensure_ascii=False, indent=2)
    print(f"Wrote sigil inventory to {out_path}")
    print(counts)


def main():
    almost_main()


if __name__ == "__main__":
    main()
