# Initially generated by GitHub Copilot.
"""
HTML document: Dual-trope - מ:כפול (plain format).

To regenerate the output HTML, run from the repo root::

    .venv/Scripts/python.exe py/main_authored.py gen-mam-parsed-docs

Output goes to gh-pages/MAM-parsed/plain/html/mpplain_dualcant.html.
"""

from mb_author import author
from mb_author.claim import ClaimCollection
from author_misc import mp_cmn_json_snippets as jsnip
from author_misc import mp_dualcant_common as dualcant_common

_FNAME = "mpplain_dualcant.html"
_TITLE = "Dual-trope (plain format)"

_PLAIN_DOC = "mpplain.html"

_JSON_DUALCANT = jsnip.read_text("mpplain_dualcant", "dualcant.json")


def gen_html_file(tdm_ch, claims: ClaimCollection):
    """Generate mpplain_dualcant.html in the given output directory."""
    cbody = build_body(claims=claims)
    return author.help_gen_html_file(__file__, tdm_ch, _FNAME, _TITLE, cbody)


def build_body(*, claims: ClaimCollection):
    return dualcant_common.build_body(
        claims=claims,
        back_doc=_PLAIN_DOC,
        back_label="Reading $MAM-parsed-plain",
        heading_suffix=" (plain format)",
        claim_id="mp.plain.example.dualcant-template-stmpl",
        claim_kind="example",
        claim_subject="mp:plain",
        example_json=_JSON_DUALCANT,
    )


def _build_body(*, claims: ClaimCollection):
    return build_body(claims=claims)
