# Initially generated by GitHub Copilot.
"""Shared FOI URL helpers for pasleg authoring modules."""

from mb_cmn.url_percent import pct_fragment

_FOI_PASOLEG_1_URL = (
    "https://bdenckla.github.io/MAM-basics/MAM-with-doc/foi/foi-pasoleg-1.html"
)
_SPACE = "«space»"
_REVIA = "⅃"


def foi_pasoleg_intro_leg_rev_url(
    intervening_count: int,
    intervening_pattern: str | None = None,
) -> str:
    """Build FOI URL for the intro-⅃-leg...(rev) fragment grammar."""
    fragment = (
        f"intro-{_REVIA}-leg...(rev){_SPACE}" f"with{_SPACE}{intervening_count}{_SPACE}"
    )
    if intervening_pattern is not None:
        fragment += f"{intervening_pattern}{_SPACE}"
    fragment += "intervening"
    return f"{_FOI_PASOLEG_1_URL}#{pct_fragment(fragment)}"
