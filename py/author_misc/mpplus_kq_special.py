# Initially generated by GitHub Copilot.
"""
HTML document: Special ketiv/qere — מ:כו״ק מיוחד.

To regenerate the output HTML, run from the repo root::

    .venv\\Scripts\\python.exe py\\main_authored.py gen-mam-parsed-docs

Output goes to ../MAM-parsed/gh-pages/mpplus_kq_special.html.
"""

from mb_author import author
from mb_author.claim import ClaimCollection
from author_misc import mp_cmn_json_snippets as jsnip
from author_misc import mp_kq_special_common as kq_special_common

_FNAME = "mpplus_kq_special.html"
_TITLE = "Special ketiv/qere"
_CLAIM_ID_KQ_SPECIAL_PLUS = "mp.plus.kq-special.semantic-shape"

_PLUS_DOC = "mpplus.html"

_JSON_KQ_SPECIAL = jsnip.read_text("mpplus_kq_special", "kq_special.json")


def gen_html_file(tdm_ch, claims: ClaimCollection):
    """Generate reading_mam_parsed_plus_kq_special.html in the given output directory."""
    cbody = build_body(claims=claims)
    return author.help_gen_html_file(__file__, tdm_ch, _FNAME, _TITLE, cbody)


def build_body(*, claims: ClaimCollection):
    return kq_special_common.build_body(
        claims=claims,
        back_doc=_PLUS_DOC,
        back_label="Reading $MAM-parsed-plus",
        heading_suffix="",
        claim_id=_CLAIM_ID_KQ_SPECIAL_PLUS,
        claim_kind="struct",
        claim_subject="mp:plus",
        claim_data={
            "template": "מ:כו״ק מיוחד",
            "required_positional_params": ["1", "2"],
            "required_named_params": ["סוג"],
        },
        example_json=_JSON_KQ_SPECIAL,
    )


def _build_body(*, claims: ClaimCollection):
    return build_body(claims=claims)
