# Updates to the meteg-after-silluq documentation search

State: open, first entry 2026-09-12.

This file corrects `doc/meteg-after-silluq-search-in-mam-documentation.md`, a finished report
that is left exactly as written.

## 1 Kings 7:37: Simanim means the Simanim Tanakh

Recorded by Codex on 2026-09-12. This entry corrects two passages: the 1 Kings 7:37 row of
finding 1 and the paragraph immediately after that table. Both passages say that MAM's note
identifies the Simanim Tiqqun. At both passages, “the Simanim Tiqqun” should read “the Simanim
Tanakh.”

The mirrored public introduction at `in/mam-ws-intro/appendices.mediawiki` defines `סימנים`,
under the list of editions based on the Aleppo Codex, as `תנ"ך סימנים (פלדהיים תשס"ח)`. The
introduction therefore identifies the edition directly; no inference about the Simanim Tiqqun's
haftarot is needed.

Product axis: this correction changes documentation only and reaches no generator or product.
Act axis: this update file preserves the finished report rather than rewriting it; no
outward-facing act, destructive local act, external configuration write or receipt rewrite
occurred.

## Analytic `ga'ya` terminology should read `meteg`

Recorded by Codex on 2026-09-12. This entry corrects six passages in the finished report's
analytic voice. The six corrected readings are:

1. The finding 2 category label “23: L has a ga'ya to the right of its vowel (glyph placement)”
   should read “23: L has a meteg to the right of its vowel (glyph placement).”
2. The finding 2 category label “16: a maqaf compound with a ga'ya against an accented atom”
   should read “16: a maqaf compound with a meteg against an accented atom.”
3. The finding 2 category label “12: a ga'ya present or absent, or its letter, in one manuscript”
   should read “12: a meteg present or absent, or its letter, in one manuscript.”
4. The finding 2 category label “6: two ga'yot before the stress in one manuscript or printing”
   should read “6: two metegs before the stress in one manuscript or printing.”
5. The finding 4 statement beginning “Every other roster chanted word that MAM has with the
   ga'ya after the accent” should read “Every other roster chanted word that MAM has with the
   meteg after the accent.”
6. The fifth item under “What could not be verified,” beginning “a passage that describes a
   silluq with a ga'ya after it,” should read “a passage that describes a silluq with a meteg
   after it.”

These six passages use the report's analytic voice, for which the report's opening vocabulary
note declares “meteg.” The passages that report MAM's introduction, Yeivin's `gaʿya`, or
Breuer's `ga'aya` remain unchanged. All twelve `ga'ya` sites in
`doc/foi-mtgmtg-empty-cell.md` also remain unchanged because each reports Yeivin or his
categories. Finding 11.5's separate choice between `hataf` and `ḥataf` remains unmade.

Product axis: this correction changes documentation only and reaches no generator or product.
Act axis: this update file preserves the finished report rather than rewriting it; no
outward-facing act, destructive local act, external configuration write or receipt rewrite
occurred.
