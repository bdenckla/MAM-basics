# Updates to the mega-coverage plan

State: open, first entry 2026-09-12. Every entry here corrects
`doc/PLAN-mega-coverage.md`, which is left exactly as written.

Ben's decision, 2026-09-11: a finished dated document — a review, a remediation plan, a completed
plan, an execution record — is left as written. A correction or later disposition goes in a
sibling file named `<stem>-update.md`, which is what this file is for that plan.

## Phase 7's run mode and unrun mode

Recorded by Codex on 2026-09-12 for finding 20.3 of
`doc/review-findings-2026-09-10.md`. The phase 7 item 3 sentence beginning “Where the mega runs one
flag-selected mode of a program and not another” should read:

> Where the mega runs a flag-selected mode of a program but does not run a different flag-selected
> mode of the same program, declare the mode that the mega does not run by hand.

The replacement names the mode that the mega runs and the mode that the mega does not run directly.
