# Updates to the meteg-after-silluq screen against UXLC and WLC

State: open, first entry 2026-09-12. Every entry here corrects or supplements
`doc/meteg-after-silluq-screen-against-uxlc-and-wlc.md`, which is left exactly as written.

Ben's decision, 2026-09-11: a finished dated document — a review, a remediation plan, a completed
plan, an execution record — is left as written. A correction or later disposition goes in a
sibling file named `<stem>-update.md`, which is what this file is for that screen report.

## The two opening terms are a numbered list

Recorded by Codex on 2026-09-12, for finding 20.4 of
`doc/review-findings-2026-09-10.md`. The opening sentence beginning “Two terms” defines both terms
in running prose. The two definitions should be read as this numbered list:

1. An **atom** is one written word between spaces or maqafs.
2. A **chanted word** is a lone atom or a whole maqaf compound.

The finished screen report remains unchanged; this entry corrects only the presentation of the
two definitions.

## The four remaining summary leads put their dispositions first

Recorded by Codex on 2026-09-12, for finding 20.5 of
`doc/review-findings-2026-09-10.md`. The four cited leads are the report's analytic prose, not
protected quotations. Each disposition is already supported by the screen or the explanation
that follows the lead. The leads should be read as follows:

1. The lead beginning “The target class has 6 members against UXLC 3.9” should read:
   **Established by the screen: the target class has 6 members against UXLC 3.9 and the same 6
   against WLC 4.22 — 5 beyond 1 Kings 7:37.**
2. The lead beginning “Phonetic MAM cannot arbitrate the two undecided members” should read:
   **Not used as evidence: Phonetic MAM cannot arbitrate the two undecided members because its
   verdict restates the mark placement rather than testing the mark placement.**
3. The lead beginning “The reverse class (v) has 4 members” should read: **Established by the
   screen: the reverse class (v) has 4 members against UXLC 3.9 and 3 against WLC 4.22, with
   1 Samuel 17:5 among the members for both transcriptions.**
4. The lead beginning “mgketer's transcription of Mikra'ot Gedolot ha-Keter is a usable third
   screen” should read: **Established by the third screen: mgketer's transcription of Mikra'ot
   Gedolot ha-Keter is usable as a third screen, and class (iii) is empty against mgketer.**

The finished screen report remains unchanged; these corrections change only the order in which
the four leads state their existing dispositions.

## Finding 11.5: narrative romanizations should use `pataḥ`, `ḥataf` and `deḥi`

Recorded by Codex on 2026-09-13. A fresh census of the finished screen report found the 17
legacy romanizations named by finding 11.5: 4 `patax` sites, 7 `xataf` sites and 6 `dexi` sites.
The corrected readings are:

1. In the summary paragraph beginning “4 of the 6 are settled by mark position alone,” every
   narrative `patax`, `xataf` and `dexi` should read `pataḥ`, `ḥataf` and `deḥi`, respectively.
   This correction covers two `patax` sites, four `xataf` sites and three `dexi` sites.
2. In section 3's **Syllables** item, “a furtive patax” should read “a furtive pataḥ,” and “a
   sheva or a xataf” should read “a sheva or a ḥataf.”
3. In section 3's **Nuclei** item, each of the two `xataf` sites should read `ḥataf`, and “a
   furtive patax” should read “a furtive pataḥ.”
4. In section 8, the sentence immediately before the table and both the Psalms 4:3 and Psalms
   71:9 table rows should each read “deḥi” in place of “dexi.”

The finished screen report's substantive bytes match historical blob
`a46dbf0f9b384afa072878584c1dace2328dc179`; its only additional line is the authorized update
pointer.
